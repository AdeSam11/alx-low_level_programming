#include "main.h"

/**
 * add - check description
 * Description: a function that adds two integers and returns the result
 * @x: one of the input integers
 * @y: one of the input integers
 * Return: sum of x and y
 */
int add(int x, int y)
{
	int sum = x + y;
	return (sum);
}
